model:
  name: 'https://tfhub.dev/google/trillsson5/1'

data:
  develop: false
  batch_size: 4
  root_dir: '/netscratch/herzig/datasets/BabyCry'
  n_fold: 11
  shuffle: true

mode: 'train'

train_config:
  epochs: 20
