model:
  name: 'https://tfhub.dev/google/trillsson5/1'

data:
  develop: false
  batch_size: 4
  root_dir: '/netscratch/herzig/datasets/BabyCry'
  shuffle: true

mode: 'train'

train_config:
  epochs: 2
